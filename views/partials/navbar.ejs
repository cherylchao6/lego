<nav class="bg-sky-600 text-white">
  <div class="container mx-auto px-4 md:flex items-center gap-6">
    <!-- Logo -->
    <div class="flex items-center justify-between md:w-auto w-full">
      <a href="/" class="py-5 px-2 text-white flex-1 font-bold"
        >Lego Collection</a
      >

      <!-- mobile menu icon -->
      <div class="md:hidden flex items-center">
        <button type="button" class="mobile-menu-button">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            class="w-6 h-6"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25H12"
            />
          </svg>
        </button>
      </div>
    </div>

    <div class="">
      <ul
        class="hidden md:flex md:flex-row flex-col items-center justify-start md:space-x-1 pb-3 md:pb-0 navigation-menu md:ml-auto md:space-x"
      >
        <li class="pr-8">
          <a
            href="/about"
            class="<%= typeof page !== 'undefined' && page === '/about' ? 'active' : '' %>"
            >About</a
          >
        </li>
        <li>
          <a
            href="/lego/sets"
            class="<%= typeof page !== 'undefined' && page === '/lego/sets' ? 'active' : '' %>"
            >View Collections</a
          >
        </li>
        <% if (session && session.user) { %>
        <li>
          <details>
            <summary>Account: <%= session.user.userName %></summary>
            <ul class="p-2 right-0">
              <li>
                <a
                  class="<%= typeof page !== 'undefined' && page === '/lego/addSet' ? 'active' : '' %>"
                  href="/lego/addSet"
                  >Add to Collection</a
                >
              </li>
              <li>
                <a
                  class="<%= typeof page !== 'undefined' && page === '/userHistory' ? 'active' : '' %>"
                  href="/userHistory"
                  >User History</a
                >
              </li>
              <li><a href="/logout">Log Out</a></li>
            </ul>
          </details>
        </li>
        <% } else { %>
        <li>
          <details>
            <summary>Account</summary>
            <ul class="p-2 right-0">
              <li>
                <a
                  class="<%= typeof page !== 'undefined' && page === '/login' ? 'active' : '' %>"
                  href="/login"
                  >Login</a
                >
              </li>
              <li>
                <a
                  class="<%= typeof page !== 'undefined' && page === '/register' ? 'active' : '' %>"
                  href="/register"
                  >Register</a
                >
              </li>
            </ul>
          </details>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
<script src="/public/scripts/home.js"></script>
